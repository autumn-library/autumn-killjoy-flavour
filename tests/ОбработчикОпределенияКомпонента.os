#Использовать autumn
#Использовать asserts
#Использовать ".."
#Использовать "."

Функция ПостроитьПоделку()

	Поделка = Новый Поделка();
	Поделка.ЗапуститьПриложение();

	Возврат Поделка;

КонецФункции

&Тест
Процедура ОбработчикПолучаетОпределенияЖелудей() Экспорт
	// Дано
	ПутьЛог = ОбъединитьПути(КаталогВременныхФайлов(), "killjoy_priemka_test.log");
	Если Новый Файл(ПутьЛог).Существует() Тогда
		УдалитьФайлы(ПутьЛог);
	КонецЕсли;

	// Когда
	ПостроитьПоделку();

	// Тогда
	Ожидаем.Что(Новый Файл(ПутьЛог).Существует(), "Обработчик определения компонента должен сработать")
		.Равно(Истина);
	Текст = Новый ЧтениеТекста(ПутьЛог).Прочитать();
	Ожидаем.Что(СтрНайти(Текст, "СобирательОпределений") > 0, "Обработчик должен получать определения желудей")
		.Равно(Истина);
КонецПроцедуры
